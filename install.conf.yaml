- defaults:
    link:
        relink: true

- clean: ['~']

- link:
    #~/.config/fish/config.fish:
    ~/.hyper.js:
    ~/.eslintrc:
    #~/.vscodestyles.css:
    ~/.zshrc:

- shell:
  - [git submodule update --init --recursive, Installing submodules]
