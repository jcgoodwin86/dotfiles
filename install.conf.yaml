- defaults:
    link:
      relink: true

- clean: ["~"]

- link:
    #~/.hyper.js:
    ~/.eslintrc:
    #~/.vscodestyles.css:
    ~/.zshrc:

- shell:
    - [git submodule update --init --recursive, Installing submodules]
